REQUIREMENTS RUBRIC

* Open the calendar, it should be showing the current month.
how i did i test this: When open live server It shows you february 2026 which is the current and the current year and the are no errors in dev tools.

* Go to October 2024. Observe:
  * A grid of 5 rows x 7 columns.
  * The first row contains Tuesday October 1 - Sunday October 6. Sunday and Monday are either labelled for September 29/30 or are blank.
  * The last row contains Monday October 28 - Thursday October 31. Friday  and Saturday are either labelled for November or are blank.
  * October 8th: Ada Lovelace Day.
  * October 25th: World Lemur Day.

HOW DID I TEST THIS: 

I used the month selector and year input to go to October 2024, and I visually inspected that our rendered days align with the relevant month, like October 1st aligning with Tuesday, etc . 
I confirmed that Monday and Sunday are empty as they are only labeled in September . 
I also checked that our build events function and confirmed that our build events function successfully mapped the holiday to the specific days which were.
October 8 and 25 and correctly upended them to the relevant cells.

* On the web page, open October 2020. Observe:
  * October 13th: Ada Lovelace Day.
  * October 30th: World Lemur Day.

HOW DID I TEST THIS: 
I went to select year/month function and navigated to the year 2020
with buildevents function in our utilities file common.mjs ,
 i used console log in the terminal and put in mock dta to verify if it processed the vent list for the year we are given. 
In the UI i observed that the cell for 13 and 30 were dynamically updated with correct event titles showing us that the DOM logic was correctly retrieving data

* On the web page, open May 2030. Observe:
  * May 11th: International Binturong Day.

HOW DID I TEST THIS: I navigated to the may 2030 suing select year buttons, 
I navigated to May 2030 using the month and year selectors.
 I verified that on May 11th, the 'International Binturong Day' event was correctly rendered within the day cell. 
 To confirm the logic, I used Chrome DevTools to inspect the DOM element for that day, ensuring the event text was dynamically injected as expected.
 This test confirms that my buildEventsByDateMap function successfully fetches data from the source file and maps it to the correct date, even for years in the distant future.

* The following months start and end on these dates, and do not have extra days or padding boxes outside of the weeks they're meant to cover:
  * 2024-12: Sunday (no empty days before) - Tuesday (4 empty days after).
  * 2025-02: Saturday (6 days before) - Friday (1 empty day after).
  * 2025-05: Thursday (4 empty days before) - Saturday (no empty days after).
  * 2026-02: Sunday (no empty days before) - Saturday (no empty days after).

HOW DID I TEST THIS: 

I manually went to each month and year to verify the grid structure.
I referred to the day 1 position of each month in a Google Calendar to ensure our logic that gets the first month was accurate.
I confirmed that in Feb 2026 the month starts on Sunday and there are zero empty leading boxes, and for May 2025 the month starts on Thursday and there are 4 empty leading boxes.
I also used the dev tool to confirm that no extra ghost rows beyond the required weeks were added and our grid remained clean and compact.

* Functioning previous and next buttons including at the end-points of the month+year selector (if it has ends).
  * If there are end-points to the month+year selector, the previous and next buttons must work flawlessly when going beyond that limit. The UI must not e.g. show an empty string, "undefined", "null", "NaN" or similar if the "current month" or "current year" is displayed anywhere in the UI. The calendar days must be correctly shown.

HOW DID I TEST THIS:
 I navigated to the December 2050 end year and clicked the "Next Month" button and make sure it works.

 *A usable way of jumping to a month+year.
  
HOW DID I TEST THIS:  
I tested the dropdown month selector and year input and selected random combinations to ensure the calendar jumped immediately to the correct view.

I also ensured that when I jump to a year, our state reflects the jump and our displayCalendar function is triggered to render the grid accordingly.

* The website must score 100 for accessibility in Lighthouse
HOW DID I TEST THIS: We ran the Light house report on dev tools and our project 100% accessibility for both views mobile and desktop
36
* Unit tests must be written for at least one non-trivial function
HOW DID I TEST THIS: 
We utelised a common.mjs file and imported the functions we wanted to test using node, 
we wrote 2 test one that checked that a specific leap year has 29 days and the one that checks 
that function incrementMonth wraps from December to January when a user clicks next month button in December and they are within the year boundaries we defined. 